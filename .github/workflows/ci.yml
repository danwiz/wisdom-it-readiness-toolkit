name: Toolkit CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Run unit tests
        run: python -m unittest discover -s tests -v
      - name: Score synthetic assessment
        run: python src/cli.py score assessments/quick-check-v0.1.json examples/synthetic-responses.json
      - name: Generate example report
        run: |
          python src/cli.py report assessments/quick-check-v0.1.json examples/synthetic-responses.json \
            --subject "Synthetic Demonstration" \
            --data-note "Synthetic demonstration only" \
            --output build/example-report.md
          test -s build/example-report.md
      - uses: actions/upload-artifact@v4
        with:
          name: example-readiness-report
          path: build/example-report.md
